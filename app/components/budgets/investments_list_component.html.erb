<% if investments.any? %>
  <section id="investments-list" class="investments-list">
    <header>
      <h2><%= t("budgets.show.investments_list") %></h2>
    </header>

    <% investments.each do |investment| %>
      <%= render Budgets::InvestmentComponent.new(investment) %>
    <% end %>
  </section>
<% end %>

<% unless budget.informing? %>
  <%= render "budgets/all_investments_link", budget: budget %>
<% end %>
