<div class="small-12 column">
  <%= back_link_to %>

  <h2><%= t("management.import_users") %></h2>
</div>

<div class="moderated-text-imports-form">
  <%= render "form" %>
  <%if !@logs.blank?%>
  <div class="small-12 column import-errors">
    <% @logs.each do |k,v| %>
    <p><%= "Fila #{k}: #{v}"%></p>
    <% end %>
  </div>
  <%end%>
</div>
