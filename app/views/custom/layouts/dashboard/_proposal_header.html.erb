<h3 class="proposal-title">
  <%= link_to proposal.title, proposal, target: "_blank" %>
</h3>

<%= render "proposals/status_label", proposal: proposal %>

<% if content_for?(:action_title) %>
  <h3 class="title"><%= yield(:action_title) %></h3>
<% end %>
